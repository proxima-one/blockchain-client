
var asser = require("assert");

describe('ethereum blockchain client', function() {

  //initialize with/without handlers and clients, getter and setter functions
  it('should initialize ethereum blockchain client', function() {
    assert.equal(true, false);
  });
  //imports event handlers, and can include smart contracts
  it('should be able to', function() {
    assert.equal(true, false);
  });
  //blockchain smart contract listener

  //blockchain event listener

  //blockchain transaction listener

  //blockchain fn listener

  //blockchain blockHandler

  //should be able to handle multiple clients at the same time?

  //should be able to fit into the generalized blockchain "schema"
});
